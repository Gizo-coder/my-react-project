
const Buttons = ({ColorUp, ColorDown}) => {

    
    return (
        <div>
            <button onClick={ColorUp}>+</button>
            <button onClick={ColorDown}>-</button>
        </div>
    )
}

export default Buttons